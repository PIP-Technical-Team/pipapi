---
plan: null
findings:
  P0.1: fixed
  P1.1: fixed
  P1.2: fixed
  P2.1: fixed
  P3.1: fixed
---

## Review Report

**Review depth**: light + `@cg-version-control` (auto-escalated: auth/credential files changed)  
**Files reviewed**: 6 (`.Rbuildignore`, `R/duckdb_func.R`, `inst/TMP/TMP_duckdb_cache.R`, `inst/plumber/v1/endpoints.R`, `inst/plumber/v1/openapi.yaml`, `tests/testthat/test-reset_cache.R`)  
**Findings**: 1 × P0, 2 × P1, 1 × P2, 1 × P3

---

### P0 — BLOCKING (immediate remediation required)

- **[P0.1]** [cg-testing] `tests/testthat/test-reset_cache.R:218` — "no-file" test will error in CI.
  **Why**: `withr::local_envvar(PIP_CACHE_SERVER_KEY = "test-key")` sets only the server key. `.check_cache_auth()` independently checks `!nzchar(Sys.getenv("PIP_CACHE_LOCAL_KEY", unset = ""))` — if `PIP_CACHE_LOCAL_KEY` is absent from the environment (as it will be in CI), the abort fires and `expect_no_error()` fails. The P3.4 fix that removed `PIP_CACHE_LOCAL_KEY` from this test's `local_envvar` was incorrect.
  **Fix**: Restore `PIP_CACHE_LOCAL_KEY = "test-key"` to this test's `withr::local_envvar()` call.

---

### P1 — CRITICAL (must fix before merge)

- **[P1.1]** [cg-version-control] `inst/TMP/TMP_duckdb_cache.R` — file still tracked by git.
  **Why**: `inst/TMP/.gitignore` excludes `/TMP` (subdirectory) and `TMP_povline_vectorization.R`, but not `TMP_duckdb_cache.R`. The root `.gitignore` also does not cover it. The original P1.1 fix added `^inst/TMP$` to `.Rbuildignore` (preventing it from shipping in the package tarball) but did not add it to `.gitignore`. The file continues to be committed and visible to all contributors.
  **Fix**: Add `TMP_duckdb_cache.R` to `inst/TMP/.gitignore`.

- **[P1.2]** [cg-testing] `tests/testthat/test-openapi-spec.R:13` — uses `here::here()` but `here` is not in `DESCRIPTION` Suggests.
  **Why**: When `system.file()` returns `""` (package not installed), the test falls back to `here::here(...)`. `here` is not listed in `DESCRIPTION` Suggests — only `rprojroot` is. On a clean CI runner, `here` will not be installed and the test errors with "there is no package called 'here'".
  **Fix**: Replace `here::here(...)` with `rprojroot::find_package_root_file(...)` (already a Suggest) or add `here` to `DESCRIPTION` Suggests.

---

### P2 — IMPORTANT (should fix)

- **[P2.1]** [cg-testing] `tests/testthat/test-reset_cache.R:137` — hardcoded Windows-absolute path in a mock test.
  **Why**: `cache_file_path = "E:/PIP/pipapi_data/20260430_2017_01_02_INT/cache.duckdb"` is passed to a mocked `treat_cache_and_main()` call. The mock intercepts before the path is accessed on disk, so the test passes on all platforms — but the path appears in test output and `git blame`, referencing a machine-specific directory.
  **Fix**: Replace with `fs::path(withr::local_tempdir(), "cache.duckdb")`.

---

### P3 — MINOR (nice to have)

- **[P3.1]** [cg-code-quality] `R/duckdb_func.R:412` — single-quote string in `reset_cache()`.
  **Why**: `fs::path(lkup$data_root, 'cache', ext = "duckdb")` uses single quotes, inconsistent with the rest of the file and the double-quote style standardised in the P3.2 fix.
  **Fix**: Change to `fs::path(lkup$data_root, "cache", ext = "duckdb")`.

---

### ✅ Passed

- **cg-code-quality**: `.check_cache_auth()` helper well-structured; `cli::cli_abort()` used consistently; `fs::file_exists()` replacing `file.exists()`; double-quoted default args; no DRY violations introduced.
- **cg-version-control**: No credentials or secrets in tracked files; `Sys.setenv()` removed from TMP script; `.Rbuildignore` updated; `.gitignore` gap flagged and fixed under P1.1.
- **cg-testing** (partial): Auth-failure tests thorough and correct; partial-sidecar and return-value contract tests solid; TMP live test correctly guarded behind env var.
