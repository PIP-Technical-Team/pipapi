% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pip_lineups_postprocess.R
\name{pip_lineups_format_output}
\alias{pip_lineups_format_output}
\title{Format and finalise pip lineups output}
\usage{
pip_lineups_format_output(
  out,
  lkup,
  fill_gaps,
  reporting_level,
  censor,
  additional_ind,
  use_old_dist_stats = FALSE
)
}
\arguments{
\item{out}{data.table: main output data passed in from the caller}

\item{lkup}{list: a versioned lkup list (same object as in callers)}

\item{fill_gaps}{logical: forwarded from the caller}

\item{reporting_level}{character: forwarded from the caller}

\item{censor}{logical: forwarded from the caller}

\item{additional_ind}{logical: forwarded from the caller}

\item{use_old_dist_stats}{logical: if \code{TRUE} use \code{\link[=add_dist_stats_old]{add_dist_stats_old()}}
(frozen old pathway); if \code{FALSE} (default) use \code{\link[=add_dist_stats]{add_dist_stats()}}
(new pathway). Default is \code{FALSE}.}
}
\value{
data.table with final output columns, ordered and de-duplicated
}
\description{
Shared post-processing tail used by both \code{\link[=pip_new_lineups]{pip_new_lineups()}} and
\code{\link[=pip_old_lineups]{pip_old_lineups()}}.  Responsibilities (in order):
\enumerate{
\item Attach pre-computed distributional statistics.
\item Attach aggregate medians.
\item Zero out distributional vars and set \code{estimate_type} for lineup years.
\item Filter to the requested \code{reporting_level}.
\item Censor country rows when \code{censor = TRUE}.
\item Optionally add additional indicators.
\item Keep only the relevant output columns.
\item Round all doubles to 12 significant digits.
\item Sort by \code{country_code}, \code{reporting_year}, \code{reporting_level},
\code{welfare_type}.
\item Remove duplicates with \code{\link[collapse:funique]{collapse::funique()}}.
}
}
\keyword{internal}
